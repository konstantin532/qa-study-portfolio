# Этап 1: модальные окна

Область исправления ограничена `#modals`. Компонент `#context-panel` на этом этапе не изменён.

Исправлено: центрирование относительно viewport; ограничение высоты; безопасные отступы; устранение прокрутки у `.modal-dialog` и `.modal-content`; прокрутка только в `.modal-body`; однозначное поведение атрибута `hidden`.

Проверка: откройте модальное окно и выполните `tests/modal-stage1-console-test.js` в DevTools Console. Ожидается `STAGE 1 PASS`; допустимое отклонение центра — не более 2 px.
